const Footer = ({ theme }) => {
   return (
      <footer theme={theme}>
         &copy; 2026, Bintang Al Fizar | <span>| STAR</span>
         <span>DEVS</span>
      </footer>
   );
};

export default Footer;
